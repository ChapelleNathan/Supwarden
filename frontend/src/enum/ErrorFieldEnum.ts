export enum ConnectionErrorEnum {
    FIRSNAME,
    LASTNAME,
    PHONENUMBER,
    PASSWORD,
    CONFIRMPASSWORD,
    EMAIL,
    DEFAULT
}

export enum CreatePasswordEnum {
    PASSWORD,
    NAME,
    IDENTIFIER,
    URI,
}
